---
summary: "openmeteo_dataframe_query surfaces declared recovery hints and points missing-table errors at dataframe_describe; truncated preview headings report the staged record_count; get_ensemble previews skip leading null past_days rows; mcp-ts-core ^0.10.14"
breaking: false
security: false
---

# 0.2.1 — 2026-07-06

## Changed

- **`openmeteo_get_ensemble` — truncated preview favors rows with data.** `past_days` responses lead with all-null placeholder rows the models don't hindcast, so a chronological head-drain preview could be entirely null while the staged canvas held usable forecast rows. The inline preview now starts at the first row carrying a non-null value; the canvas still stages every row in chronological order, and the rendered text discloses that any omitted leading null rows are staged in full. ([#14](https://github.com/cyanheads/open-meteo-mcp-server/issues/14))
- **Dependencies:**
  - `@cyanheads/mcp-ts-core` `^0.10.10` → `^0.10.14`
  - New `@socketsecurity/bun-security-scanner` `^1.1.2` devDependency, paired with a `bunfig.toml` supply-chain guard (3-day `minimumReleaseAge` install hold, scanned installs) adopted from the framework template.
- **Framework sync.** 12 project skills and 4 `scripts/` files (`build-changelog`, `devcheck`, `lint-mcp`, `tree`) re-synced to `mcp-ts-core` 0.10.14; `devcheck`'s outdated-package gate now recognizes rows held back by `minimumReleaseAge` instead of flagging them as unaddressed; Dockerfile pins `oven/bun:1.3.14` and installs with `--ignore-scripts` plus BuildKit cache mounts.

## Fixed

- **`openmeteo_dataframe_query` — recovery hints reach the wire.** Framework `system_catalog_access` and `missing_table` query errors are now rewrapped under the tool's own declared error contracts: the declared recovery hint is attached (the framework's `system_catalog_access` throw carried none), and a missing-table error names `openmeteo_dataframe_describe` instead of the framework's internal `registerTable()`/`describe()` methods. Every other query error (`invalid_sql`, a TTL-expired `canvas_not_found`, …) passes through unchanged. ([#12](https://github.com/cyanheads/open-meteo-mcp-server/issues/12))
- **Truncated preview headings report the staged `record_count`.** `openmeteo_get_historical`, `openmeteo_get_ensemble`, and `openmeteo_get_climate` labeled a truncated preview's heading with the preview slice's own length (e.g. "first 2 of 2") instead of the full row count staged on the canvas; all three now cite `record_count`. ([#13](https://github.com/cyanheads/open-meteo-mcp-server/issues/13))
